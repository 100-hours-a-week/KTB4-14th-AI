# AUDIGO AI 개발 기록
팀원: lina (풀스택), amon(클라우드), sky(클라우드), gourmet(풀스택)

마지막 정리: **2026-09-17 · 한국 시간 기준**

날짜별로 무엇을 개발했고, 어떤 요청을 반영했으며, 팀원에게 무엇을 전달해야 하는지 기록한다. 새 작업은 이 파일의 날짜별 기록에 추가한다. `완료`는 AI 코드에서 구현·검증한 상태이며, 서비스 전체 연동과 운영 배포는 별도로 표시한다.

## 팀원에게 현재 상태를 설명할 때

> 9월 16일까지는 AI API의 기본 구조와 명세를 정리했습니다. 9월 17일에는 카카오의 실제 장소 후보를 OpenAI가 조합해 여행 일정을 만들도록 연결했습니다. 필수 장소와 여행 날짜·시간을 검증하고, 추천 화면에 맞춰 장소·식당 → 숙소 → 이동 구간 → 음악 순서로 결과를 보내는 API도 추가했습니다. 이제 백엔드와 프론트에서 이 단계별 응답을 연결해야 합니다. 최종 일정 저장은 백엔드가 담당합니다.

## 2026-09-17 — 실제 일정 생성 및 추천 진행 단계 구현

### 받은 요청과 반영 결과

아래는 이번 개발 대화에서 받은 요청이다. 풀스택·클라우드 담당자가 별도로 승인한 사항으로 간주하지 않는다.

| 받은 요청 | 반영 내용 | 상태 |
| --- | --- | --- |
| 기존 일정 생성 명세를 기준으로 실제 추천 결과 반환 | 기존 요청 필드를 유지하고 카카오 장소 검색과 OpenAI 일정 생성을 연결 | 완료 |
| Gemini 대신 추가한 OpenAI 키 사용 | `OPENAI_API_KEY` 사용, 기존 `OPEN_API_KEY` 이름도 지원 | 완료 |
| 필수 장소, 여행 기간, 속도, 이동수단, 테마, 음식 취향 반영 | 후보 검색·모델 입력·일정 계산에 반영하고 필수 장소 누락 및 시간 초과 검증 | 완료 |
| 관광·식당·숙소로 일정 구성 | 카카오 장소를 세 카테고리로 분류하고 날짜별 방문 순서 구성 | 완료 |
| 여유롭게/빽빽하게에 따라 일정 차이 적용 | 하루 방문 수와 체류시간 정책을 다르게 적용 | 완료 |
| `reason`, `source_category` 응답 제외 | 일반 JSON 및 단계별 응답에서 제거. 원본 카테고리는 내부 분류에만 사용 | 완료 |
| 장소·식당 결과 → 숙소 결과 → 이동 경로 결과 → 음악 결과 순서로 전달 | 실제 단계가 끝날 때 결과를 전달하는 스트리밍 API 추가 | AI 구현 완료, 화면 연동 필요 |
| AI에서 `travel_plan`을 저장하지 않기 | 생성 결과만 반환. 확정 저장은 별도 백엔드 API의 책임 | 반영 |
| V1 범위, `development` 안에서 개발 | 현재 작업은 V1 AI API에 한정 | 반영 |
| 배포에 필요한 파일만 쉽게 구분 | 아래 클라우드 전달 사항에 실행 파일·설정·검증 자료의 역할 정리 | 정리 완료 |
| `API_FLOW.md` 대신 날짜별 개발 기록 하나로 관리 | 기존 파일을 없애고 연동 설명까지 이 파일로 통합 | 완료 |

### 오늘 개발한 내용

**일정 생성:** 카카오에서 실제 장소 후보를 조회하고, OpenAI에는 후보 중 방문할 장소와 날짜별 순서를 선택하도록 요청한다. 장소 이름·좌표를 모델이 임의로 만들어 응답하지 않도록 후보 ID를 검증한다. 필수 장소는 요청에 포함된 정보를 바탕으로 유지하며, 백엔드는 해당 장소의 ID·이름·좌표가 일치하는 정보를 전달해야 한다.

**시간과 검증:** 도착·출발 일시 안에서 체류시간과 추정 이동시간을 계산한다. 중간 날짜의 기본 활동 시간은 09:00~21:00이며, 첫날과 마지막 날은 도착·출발 시간으로 제한한다. 모델의 JSON 형식과 후보 ID, 필수 장소 누락, 날짜별 일정, 방문 순서 및 시간 범위를 검증한다. 잘못된 생성 결과는 최대 한 번 재생성하고, 계속 실패하면 오류로 반환한다.

**진행 화면용 응답:** 기존 최종 JSON API를 유지하면서 단계별 결과를 받을 수 있는 SSE API를 추가했다. SSE는 한 HTTP 연결에서 결과를 순서대로 보내는 방식이다. 각 단계의 완료 결과를 전송한 뒤 다음 단계가 실행된다. 화면의 체크 표시나 화면 이동은 이 이벤트에 연결해야 한다.

**음악 추천:** 백엔드가 전달한 음악 후보 중 OpenAI가 한 곡을 선택한다. 선택한 후보의 기존 음악 ID·제목·가수·URL을 반환한다. SSE 방식과 스트림 요청의 `music_candidates` 추가는 이번 구현에서 선택한 방식으로, 백엔드·프론트와 계약 확인이 필요하다.

### 수정·추가한 주요 파일

경로는 `development/` 기준이다.

| 파일 | 담당 역할 |
| --- | --- |
| `ai_service/main.py`, `auth.py`, `errors.py` | API 진입점, 내부 인증, 공통 오류 응답 |
| `ai_service/config.py`, `.env.example` | 환경변수 및 외부 호출 제한 시간 설정 안내 |
| `ai_service/schemas.py` | 기존 요청, 일정 결과, 단계별 응답, 음악 후보 형식 |
| `ai_service/places.py` | 카카오 장소 검색·분류 |
| `ai_service/model.py` | OpenAI 호출, 구조화된 JSON 출력, 음악 후보 선택 |
| `ai_service/features.py` | 일정 구성, 체류·이동시간 계산 및 결과 검증 |
| `ai_service/pipeline.py` | 장소·식당 → 숙소 → 이동 구간 → 음악 처리 |
| `ai_service/streaming.py` | 단계별 이벤트, 오류 종료, 연결 종료 시 작업 취소 |
| `pyproject.toml`, `uv.lock` | 실행 의존성과 버전 관리 |

### 검증한 내용

| 검증 | 확인 결과 |
| --- | --- |
| 최초 일정 생성 구현 | 당시 테스트 61개 통과, 실제 카카오·OpenAI 호출로 3일 일정 HTTP 200 응답 확인 |
| 이번 응답 정리 및 4단계 처리 | 별도 검증 테스트 17개 통과. 단계 순서, 필드 제거, 필수 장소, 당일 여행, 필수 숙소, 오류 시 중단, 제한 시간·연결 취소 확인 |
| 실제 스트림 호출 | 장소·식당 → 숙소 → 이동 구간 → 음악 → 전체 완료 순서 확인. 해당 실행에서 3일 일정과 이동 구간 10개 반환 |
| 코드 정적 검사 | Ruff 검사 및 변경 내용의 공백 오류 검사 통과 |

테스트 수는 각 작업 당시의 검증 이력이다. 현재 배포 폴더에 테스트가 모두 포함되어 있다는 뜻은 아니다. 스트림 검증 코드는 배포 폴더 밖에서 실행했다. 실제 음악 카탈로그와 재생 URL의 재생 가능 여부, 프론트 화면 전환, 운영 배포는 아직 검증하지 않았다.

## 2026-09-16 — 기본 구조 및 개발 문서 정리

이 날짜의 내용은 당시 PL 미팅 기록과 Git 이력을 기준으로 작성했다.

| 진행 내용 | 당시 상태 |
| --- | --- |
| AI 레포지토리와 V1 API 기본 코드 정리 | API, 요청·응답 Schema, 인증, 환경 설정, 예외 처리의 기본 구조 준비 |
| V1 개발 가이드 및 관련 설명 문서 정리 | 개발·협업 시 참고할 기본 설명 작성 |
| 문서의 실제 구현과 향후 설계 구분 | 이미 구현한 구조와 앞으로 확장할 구조가 혼동되지 않도록 보정 |
| 실제 장소 데이터와 모델 연결 검토 | 장소·이동시간 데이터 확보와 실제 추천 로직은 추가 개발 단계 |
| 음악 추천 방식 검토 | 데모 데이터 기반 흐름에서 실제 데이터 연동으로 넘어갈 준비 단계 |

당시 미팅 문서에는 Gemini 연결 준비, Modal 실행 환경, Job 상태 관리가 기록되어 있다. **현재 `development`의 실행 구조는 9월 17일에 정리한 OpenAI·FastAPI의 일반 JSON/SSE API를 기준으로 한다.** 현재 코드에 과거 Modal 작업 큐나 상태 복구 기능이 구현되어 있다고 설명하면 안 된다.

확인 근거: [9월 16일 PL 미팅 기록](../문서/pl미팅/5회차-2026-09-16-pl-meeting%20인공지능.md), 로컬 Git의 `93f162d`, `b34a729`, `55bae60`, `ec16350` 커밋.

## 팀 요청·협의 사항과 전달할 내용

### 기존 회의에서 확인한 사항

[9월 14일 PM 미팅 기록](../문서/pm미팅/4회차-2026-09-14-pm-meeting.md)에는 다음 계획이 있다. 완료 여부를 확인한 목록은 아니다.

| 담당 | 회의에 기록된 계획 | 이번 작업과 연결되는 부분 |
| --- | --- | --- |
| 풀스택 | 여행 도메인 구현, 카카오맵 API 연동 | AI에 전달할 여행 조건·필수 장소 정보와 생성 결과 형식을 맞춰야 함 |
| 클라우드 | V1 배포 인프라·CI/CD 구축, SQS 도입 계획 | 현재 AI 서버의 실행 방법과 스트림 전달 조건 공유 필요. AI 코드에 SQS 연동이 구현된 것은 아님 |
| AI | 여행 루트 생성 모델 선정, 품질·응답시간·비용 비교 | 현재 OpenAI 모델 연동은 완료. 여러 모델의 비교 실험 완료를 의미하지는 않음 |

해당 회의에서는 서비스의 여행 추천 AI 연동을 그 주 범위에서 제외한 것으로 기록되어 있다. 이후 사용자 요청으로 AI 측 구현을 진행했으며, 팀 전체 연동 일정은 다시 맞춰야 한다. 회의 기록에 없는 풀스택·클라우드의 별도 요구를 승인된 사항으로 적지 않는다.

### 풀스택 담당자에게 전달할 사항

| 전달할 내용 | 필요한 후속 작업 |
| --- | --- |
| 기존 최종 JSON API는 계속 사용 가능 | 기존 호출부에서 제거된 `reason`, `source_category` 참조 확인 |
| 진행 화면은 새 스트림 API로 4단계 결과 수신 | 백엔드의 SSE 수신·중계와 프론트 단계 표시 연결 |
| 스트림 요청에는 `music_candidates` 필요 | 실제 음악 후보 1~100개 제공. ID 중복 금지, 기존 제목·가수·URL 전달 |
| 마지막 `complete` 이벤트가 전체 성공 | HTTP 200만으로 성공 판단하지 않고 `error` 이벤트도 처리 |
| 최종 방문 시간은 `ROUTES` 단계에서 계산 | 첫 장소 추천 단계의 결과를 최종 시간표로 사용하지 않기 |
| 최종 확정 저장은 백엔드 책임 | 사용자의 확정 요청 때 별도 API에서 일정 저장 |
| 이동 구간은 좌표 기반 추정 | 지도에 실제 도로 경로나 환승 정보를 표시할 경우 길찾기 API 연동 범위 협의 |

### 클라우드 담당자에게 전달할 사항

| 항목 | 현재 AI 코드 기준 안내 |
| --- | --- |
| 실행에 필요한 파일 | `ai_service/` 전체, `pyproject.toml`, `uv.lock`. `.env.example`은 설정 참고용 |
| 실행 예시 | `development`에서 `uv sync --frozen --no-dev` 후 `uv run --no-sync uvicorn ai_service.main:app --host 0.0.0.0 --port 8000` |
| 상태 확인 | `GET /health`. 프로세스 응답 확인용이며 외부 모델·카카오 연결 성공까지 보장하지 않음 |
| 필수 환경변수 | `AUDIGO_API_TOKEN`(32자 이상), `OPENAI_API_KEY` 또는 `OPEN_API_KEY`, `KAKAO_REST_API_KEY` |
| 모델 설정 | `OPENAI_MODEL`, 기본값 `gpt-4o-mini` |
| 키 전달 | 실제 키는 배포 환경의 환경변수/시크릿으로 주입. `.env`와 키 값을 Git·개발 기록에 포함하지 않음 |
| 스트림 전달 | 백엔드·프록시의 응답 버퍼링을 끄고 이벤트를 도착 즉시 전달. 전체 처리 240초를 수용하도록 연결 제한 시간 확인 |
| 현재 제한 시간 | 일반 생성 150초, 스트림 전체 240초, 모델 호출마다 60초, 카카오 호출마다 10초. 스트림 대기 중 10초마다 연결 유지 주석 전송 |
| 테스트·기록 파일 | 서버 실행에는 필요하지 않아 배포 이미지에서 제외 가능. 테스트 결과와 이 기록은 검증·인수인계 자료로 공유 가능 |
| 현재 범위 밖 | 운영 배포, SQS 연결, Modal Job 복구, 재연결 시 이벤트 재생은 이번 코드에서 제공하지 않음 |

## 현재 API 연동 메모

### 호출과 요청

| 용도 | API | 반환 방식 |
| --- | --- | --- |
| 최종 여행 일정만 받기 | `POST /internal/ai/itineraries/generate` | 최종 JSON |
| 추천 화면에서 단계별 결과와 음악까지 받기 | `POST /internal/ai/itineraries/generate/stream` | `text/event-stream` |

두 API 모두 `Authorization: Bearer <AUDIGO_API_TOKEN>`과 JSON 요청을 사용한다. 내부 토큰은 백엔드에서 사용한다. 스트림 요청에는 `Accept: text/event-stream`을 사용한다.

기존 요청 필드는 `generation_job_id`, `client_draft_id`, `region`, `duration`, `headcount`, `companion_type`, `preference`, `required_places`이다. 스트림에는 `music_candidates`만 추가한다. 음악 후보 한 항목은 `music_id`, `title`, `artist`, `youtube_url`로 구성하며 실제 백엔드 데이터를 전달한다.

### 처리 단계와 화면 연결

| 순서 / stage | AI가 완료한 일과 반환 결과 | 화면에 표시할 상태 |
| --- | --- | --- |
| 1 / `PLACES` | 카카오 관광·식당 검색 후 날짜별 장소와 순서 선택. `title`, `days` 반환 | 장소·식당 추천 완료 |
| 2 / `ACCOMMODATIONS` | 날짜별 장소 중심 좌표 계산, 주변 숙소 검색 및 동선 고려. `accommodations`의 날짜·검색 중심·숙소 반환 | 숙소 위치 계산 완료 |
| 3 / `ROUTES` | 숙소를 포함한 인접 장소 연결, 시간 계산·검증. `itinerary`, `routes` 반환 | 이동 경로 연결 완료 |
| 4 / `MUSIC` | 후보 중 한 곡 선택. `music_id`, `title`, `artist`, `youtube_url` 반환 | 여행 음악 추천 완료 |

단계 시작은 `stage_started`, 성공은 `stage_completed`, 전체 성공은 `complete`, 실패는 `error` 이벤트로 알린다. 이벤트의 공통 정보는 `generation_job_id`, `client_draft_id`, `request_id`, `stage`, `status`, `progress`, `data`이다. `progress`의 25/50/75/100은 완료한 단계 수의 비율이며 남은 시간 예측값이 아니다.

백엔드는 빈 줄로 구분되는 SSE 이벤트를 도착 즉시 읽고 프론트에 전달한다. POST API이므로 브라우저 기본 `EventSource`를 그대로 연결하는 방식은 사용할 수 없다. `stage_started`에서 로딩, `stage_completed`에서 완료 표시, `complete`에서 결과 화면 전환, `error`에서 실패 표시로 연결한다.

마지막 `complete.data`에는 `itinerary`(최종 일정), `routes`(이동 구간), `music`(선택된 음악)이 담긴다. 모든 응답에서 `reason`, `source_category`는 제외한다. 당일 여행은 숙소 결과가 비어 있을 수 있고, 필수 숙소가 있으면 해당 장소를 유지한다.

이동 구간에는 날짜·방문 순서, 출발/도착 장소 ID·좌표, 이동수단, 거리·소요시간, `is_estimated`가 담긴다. 전날 숙소에서 다음날 첫 장소로의 연결도 포함하며, 현재는 `is_estimated: true`이다. 영업시간·휴무일·가격·예약 가능 여부는 확인된 데이터가 아니므로 일정 응답의 안내 문구를 함께 전달한다.

### 실패와 재시도

스트림 시작 전 요청·인증·키 설정 오류는 기존 400/401/503 JSON 응답으로 처리한다. 스트림 시작 후에는 HTTP 200이 이미 전송되므로, `error` 이벤트의 실패 단계와 `data.http_status`, `data.error_message`를 확인해야 한다. 실패한 뒤 다음 단계나 `complete`는 전송하지 않는다.

연결이 끊기면 진행 중인 처리를 취소한다. 재시도는 새 생성 요청으로 처리하며, 중단 위치부터 이어받거나 과거 이벤트를 재생하는 기능은 없다. AI 서버는 단계별 결과와 최종 `travel_plan`을 DB에 저장하지 않는다.

## 다음 기록부터 남길 항목

같은 날의 작업은 해당 날짜에 이어 적고, 날짜가 바뀌면 새 날짜를 위에 추가한다. 각 날짜에 **받은 요청과 요청자 → 변경한 내용·파일 → 검증 결과 → 팀에 전달할 사항 → 남은 작업**을 기록한다. 협의 전인 내용은 `협의 필요`, 구현만 끝난 내용은 `AI 구현 완료`, 서비스에서 확인한 내용은 `연동 검증 완료`로 구분한다.
